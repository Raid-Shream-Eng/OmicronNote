{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // to import json files directly into TypeScript
    "resolveJsonModule": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"]
}
